# Baltic DNS Configuration
# Скопируйте этот файл в .env и настройте под ваш сервер

# =============================================================================
# ОСНОВНЫЕ НАСТРОЙКИ СЕРВЕРА
# =============================================================================

# IP адрес вашего сервера (используется для перенаправления DNS)
SERVER_IP=185.237.95.211

# Основной домен для размещения админки и DNS сервисов
# Например: dns.example.com, proxy.mydomain.org
HOST_DOMAIN=dns.uzicus.ru

# Поддомен для тестирования DNS резолвинга
# Будет создан как: ${TEST_SUBDOMAIN}.${HOST_DOMAIN}
TEST_SUBDOMAIN=test

# =============================================================================
# НАСТРОЙКИ LET'S ENCRYPT
# =============================================================================

# Email для получения уведомлений от Let's Encrypt
ACME_EMAIL=admin@uzicus.ru

# =============================================================================
# НАСТРОЙКИ БЕЗОПАСНОСТИ
# =============================================================================

# Пароль для доступа к админке (логин всегда "admin")
ADMIN_PASSWORD=BalticDNS2024!

# =============================================================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# =============================================================================

# Режим отладки (true/false)
DEBUG=false

# Логирование (info, warning, error)
LOG_LEVEL=info

# =============================================================================
# ВАЖНЫЕ ЗАМЕЧАНИЯ
# =============================================================================
# 
# 1. HOST_DOMAIN должен резолвиться в SERVER_IP ПЕРЕД запуском!
#    Проверьте: nslookup $HOST_DOMAIN
#
# 2. Убедитесь что порты 80, 443, 53, 853 открыты на сервере
#
# 3. Let's Encrypt сможет выдать сертификат только если домен
#    корректно настроен в DNS и доступен извне
#
# 4. Сохраните этот файл как .env (без .example)
#